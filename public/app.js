const form=document.querySelector("#exportForm");const result=document.querySelector("#result");const chainInput=document.querySelector("#chain");const countryInput=document.querySelector("#country");const formatInput=document.querySelector("#format");const sourceInput=document.querySelector("#source");const statusChip=document.querySelector("#statusChip");let pollTimer=null;const steps=[{title:"Startar export",text:"Vi skickar din begäran till exportmotorn."},{title:"Hämtar data",text:"Vi söker efter matchande platser i det valda landet."},{title:"Bearbetar adresser",text:"Vi rensar resultatet, tar bort dubbletter och standardiserar adresser."},{title:"Skapar CSV",text:"Vi förbereder filen för nedladdning."},{title:"Klar",text:"Din CSV-fil är redo att laddas ner."}];document.querySelectorAll(".example").forEach(button=>{button.addEventListener("click",()=>{chainInput.value=button.dataset.chain;countryInput.value=button.dataset.country;chainInput.focus()})});form.addEventListener("submit",async event=>{event.preventDefault();const chain=chainInput.value.trim();const country=countryInput.value.trim();const format=formatInput?formatInput.value:"CSV";const source=sourceInput?sourceInput.value:"OpenStreetMap";const submitButton=form.querySelector("button");if(!chain||!country){showError("Fyll i både kedja och land.");return}if(pollTimer){clearInterval(pollTimer);pollTimer=null}submitButton.disabled=true;setChip("Startar","running");renderStatus({currentStep:0,progress:8,badge:"Startar",title:"Startar export",subtitle:`Förbereder hämtning för ${chain} i ${country}.`});try{const response=await fetch("/api/start",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({chain,country,format,source})});const data=await response.json();if(!response.ok){throw new Error(data.error||"Kunde inte starta exporten.")}renderStatus({currentStep:1,progress:28,badge:"Arbetar",title:"Hämtar data",subtitle:`Exporten är startad. Vi hämtar platser för ${chain} i ${country}.`,actionsUrl:data.actionsUrl});startPolling({fileName:data.fileName,actionsUrl:data.actionsUrl,chain,country})}catch(error){showError(error.message)}finally{submitButton.disabled=false}});function startPolling({fileName,actionsUrl,chain,country}){let attempts=0;const maxAttempts=150;pollTimer=setInterval(async()=>{attempts+=1;const phase=getPhase(attempts);setChip(phase.badge,"running");renderStatus({currentStep:phase.step,progress:phase.progress,badge:phase.badge,title:phase.title,subtitle:phase.subtitle.replace("{chain}",chain).replace("{country}",country),actionsUrl});try{const response=await fetch(`/api/status?file=${encodeURIComponent(fileName)}`);const data=await response.json();if(!response.ok){throw new Error(data.error||"Kunde inte läsa exportstatus.")}if(data.ready){clearInterval(pollTimer);pollTimer=null;setChip("Klar","ready");renderReady({fileName,downloadUrl:data.downloadUrl,size:data.size,actionsUrl})}if(attempts>=maxAttempts){clearInterval(pollTimer);pollTimer=null;setChip("Tar längre tid","slow");renderStatus({currentStep:3,progress:82,badge:"Tar längre tid",title:"Exporten tar längre tid än väntat",subtitle:"Körningen kan fortfarande bli klar. Öppna GitHub Actions för detaljer.",actionsUrl})}}catch(error){clearInterval(pollTimer);pollTimer=null;showError(error.message)}},7000)}function getPhase(attempts){if(attempts<=2){return{step:1,progress:32,badge:"Hämtar",title:"Hämtar data",subtitle:"Vi söker efter platser för {chain} i {country}."}}if(attempts<=5){return{step:2,progress:58,badge:"Bearbetar",title:"Bearbetar adresser",subtitle:"Vi rensar, deduplicerar och standardiserar resultatet."}}return{step:3,progress:78,badge:"Skapar fil",title:"Skapar CSV",subtitle:"Vi förbereder filen. Den blir tillgänglig här så snart den är klar."}}function renderStatus({currentStep,progress,badge,title,subtitle,actionsUrl}){result.className="result";result.innerHTML=`<div class="statusPanel"><div class="statusTitle"><div><h3>${escapeHtml(title)}</h3><p>${escapeHtml(subtitle)}</p></div><div class="statusBadge">${escapeHtml(badge)}</div></div><div class="progressTrack" aria-label="Export progress"><div class="progressBar" style="width: ${progress}%"></div></div><div class="statusSteps">${steps.map((step,index)=>{const state=index<currentStep?"done":index===currentStep?"active":"";const icon=index<currentStep?"✓":index+1;return `<div class="statusStep ${state}"><div class="stepDot">${icon}</div><div><strong>${escapeHtml(step.title)}</strong><span>${escapeHtml(step.text)}</span></div></div>`}).join("")}</div>${actionsUrl?`<p class="smallText">Körningen sker i bakgrunden. <a href="${actionsUrl}" target="_blank" rel="noreferrer">Öppna teknisk logg i GitHub Actions</a></p>`:""}</div>`}function renderReady({fileName,downloadUrl,size,actionsUrl}){const sizeText=size?`${Math.max(1,Math.round(size/1024))} KB`:"CSV";result.className="result";result.innerHTML=`<div class="readyBox"><div class="statusTitle"><div><h3>CSV-filen är klar</h3><p>Exporten är färdig och kan laddas ner direkt.</p></div><div class="statusBadge">Klar</div></div><div class="progressTrack"><div class="progressBar" style="width: 100%"></div></div><div class="fileCard"><strong>${escapeHtml(fileName)}</strong><span>${escapeHtml(sizeText)}</span></div><a class="downloadButton" href="${downloadUrl}">Download CSV</a><p class="smallText">Filen är också sparad i GitHub under <strong>exports</strong>.<br><a href="${actionsUrl}" target="_blank" rel="noreferrer">Visa körningen i GitHub Actions</a></p></div>`}function showError(message){setChip("Fel","error");result.className="result";result.innerHTML=`<div class="errorBox"><strong>Något gick fel</strong><br>${escapeHtml(message)}</div>`}function setChip(text,mode){if(!statusChip)return;statusChip.textContent=text;statusChip.className="cardBadge";if(mode==="ready"){statusChip.style.background="#dcfae6";statusChip.style.color="#079455"}else if(mode==="error"){statusChip.style.background="#fff1f0";statusChip.style.color="#912018"}else if(mode==="slow"){statusChip.style.background="#fff7e6";statusChip.style.color="#b54708"}else{statusChip.style.background="#ecf7fb";statusChip.style.color="#008ca0"}}function escapeHtml(value){return String(value).replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[char]))}
